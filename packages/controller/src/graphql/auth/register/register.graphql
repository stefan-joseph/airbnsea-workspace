mutation RegisterUser(
  $email: String!
  $password: String!
  $firstName: String!
) {
  register(email: $email, password: $password, firstName: $firstName) {
    ... on SuccessResponse {
      success
    }
    ... on ValidationError {
      field
      message
    }
  }
}
