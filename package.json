{
  "private": true,
  "name": "airbnb-clone",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build:server": "lerna run build --scope={@airbnb-clone/common,@airbnb-clone/server}",
    "build:web": "CI= lerna run build --scope={@airbnb-clone/common,@airbnb-clone/controller,@airbnb-clone/web}"
  },
  "devDependencies": {
    "lerna": "^5.5.4",
    "nx": "^14.8.2"
  }
}
